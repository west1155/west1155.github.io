const initTilt = () => {
    // Initialize tilt effect
    const tiltElements = document.querySelectorAll('.tilt');
    tiltElements.forEach(el => {
        // Add tilt effect logic here, using a library like vanilla-tilt
        // Example: VanillaTilt.init(el, { max: 25, speed: 400 });
    });
};

export default initTilt;